---
layout: post
title: 用 Rand7() 实现 Rand10()
date: 2026-05-22 16:00:00 +0800
categories: algorithm
tags: algorithm
published: true
---

* content
{:toc}

## Implement Rand10() Using Rand7()

[LeetCode 470](https://leetcode.com/problems/implement-rand10-using-rand7/description/){:target="_blank"}

Given the **API** `rand7()` that generates a uniform random integer in the range `[1, 7]`, write a function `rand10()` that generates a uniform random integer in the range `[1, 10]`. You can only call the API `rand7()`, and you shouldn't call any other API. Please **do not** use a language's built-in random API.

Each test case will have one **internal** argument `n`, the number of times that your implemented function `rand10()` will be called while testing. Note that this is **not an argument** passed to `rand10()`.

**Example 1:**

```
Input: n = 1
Output: [2]
```

**Example 2:**

```
Input: n = 2
Output: [2,8]
```

**Example 3:**

```
Input: n = 3
Output: [3,8,10]
```

**Constraints:**

* 1 <= n <= $$10^5$$

**Approach 1**

```kotlin
/**
 * The rand7() API is already defined in the parent class SolBase.
 * fun rand7(): Int {}
 * @return a random integer in the range 1 to 7
 */
class Solution : SolBase() {
    fun rand10(): Int {
        var res = 40
        while (res >= 40) res = 7 * (rand7() - 1) + rand7() - 1
        return res % 10 + 1
    }
}
```

<!-- https://leetcode.com/problems/implement-rand10-using-rand7/solutions/150301/three-line-java-solution-the-idea-can-be-cf2l/ -->